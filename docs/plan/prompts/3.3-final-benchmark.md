# PR 3.3 — Final Benchmark + Upstream PR

**Epoch:** 3 (Integration & Scale)
**Depends on:** ALL previous PRs
**Estimated effort:** 0.5 weeks
**Impact:** Validation + upstream contribution

## Context

After all 8 feature PRs are merged, run comprehensive benchmarks comparing the optimized fork against the original upstream. Publish results, prepare and submit PR(s) to `safishamsi/graphify` for the features that align with their vision.

## Deliverables

### 1. Comprehensive Benchmark Run
- Run `benchmarks/runner.py` on final fork against all 5 graph scales (1k-500k)
- Run code intelligence benchmarks:
  - Call resolution accuracy on 10-repo benchmark suite (4 fixtures + 6 additional OSS repos)
  - Process trace completeness (% of entry points with non-empty traces)
  - Hybrid search relevance (NDCG@10 on 20 curated queries per repo)
  - Query latency: p50/p95/p99 for hybrid search, impact, context, trace, detect_changes
  - Memory overhead: index + cache + embeddings vs baseline
- Compare every metric against `benchmarks/baseline.json` from PR 1.1
- New file: `benchmarks/final.json` — final benchmark results

### 2. Incremental Gain Analysis
- New file: `benchmarks/analysis.md`
  - For each PR, show the delta from previous PR's benchmark
  - Identify which PRs brought the most gains (and which had regressions)
  - Table format:
    ```
    | PR | Date | Query (p50) | Query (p95) | Call Res. | Process Trace | NDCG@10 | Memory |
    |-----|------|-------------|-------------|-----------|---------------|---------|--------|
    | 1.1 | ...  | 450ms       | 1200ms      | 12%       | 0%            | 0.35    | 24MB   |
    | 1.2 | ...  | 440ms       | 1180ms      | 15%       | 0%            | 0.36    | 48MB   |
    | 1.3 | ...  | 8ms         | 45ms        | 15%       | 0%            | 0.36    | 48MB   |
    | 2.1 | ...  | 7ms         | 42ms        | 95%       | 0%            | 0.42    | 52MB   |
    | 2.2 | ...  | 7ms         | 42ms        | 95%       | 98%           | 0.52    | 62MB   |
    | 2.3 | ...  | 85ms        | 180ms       | 95%       | 98%           | 0.78    | 80MB   |
    | 3.1 | ...  | 5ms         | 15ms        | 95%       | 98%           | 0.78    | 120MB  |
    | 3.2 | ...  | 5ms         | 15ms        | 95%       | 98%           | 0.78    | 135MB  |
    ```
  - Key findings: bidirectional BFS gave 55x speedup, call resolution went from 12% to 95%, hybrid search improved NDCG from 0.35 to 0.78

### 3. Upstream PR Strategy
- Identify features suitable for upstream (align with Graphify's "any input → knowledge graph" vision):
  - Phase 1: PR the indexing layer + advanced traversal (PR 1.2 + 1.3 → generic graph optimizations, no code-specific changes)
  - Phase 2: PR the typed code schema as an optional opt-in feature (PR 1.2 code_schema.py, backward compatible)
- Code-specific features (call resolution, process tracing, MCP tools, agent integration, multi-repo) stay in fork as they diverge from upstream's heterogeneous extraction philosophy
- Draft PR descriptions with:
  - Benchmark data showing improvements
  - Architecture diagrams
  - Backward compatibility guarantees
  - Migration path for existing users

### 4. Documentation
- Update `README.md` with new features, benchmarks, and quickstart
- Update `CHANGELOG.md` with all PRs and their impacts
- Generate `docs/plan/progress.md` final summary (or update existing)

## Test Coverage Requirements

- Benchmark scripts must have 100% coverage (they're validation tools, must be correct)
- All existing tests pass (no regressions introduced in this PR)

## Validation Criteria

- [ ] `benchmarks/final.json` committed with all metrics
- [ ] Every metric improves or stays flat vs baseline (no regressions)
- [ ] `benchmarks/analysis.md` shows per-PR gains/regressions clearly
- [ ] Upstream PR draft prepared for generic optimization features
- [ ] `README.md` and `CHANGELOG.md` updated

## Commit Message

```
chore: final benchmark run, incremental gain analysis, upstream PR draft

- Run comprehensive benchmarks (BSBM + code intelligence) on final fork vs baseline
- Document per-PR incremental gains: 55x query speedup, 12%→95% call resolution, 0.35→0.78 NDCG
- Prepare upstream PR strategy: generic optimizations → upstream, code-specific → fork
- Update README.md, CHANGELOG.md with new features and benchmark data
```
